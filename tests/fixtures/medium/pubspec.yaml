name: fixture_medium
description: Medium integration fixture for dart_mutant — 4 files, ~70 mutants, mixed outcomes.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.24.0