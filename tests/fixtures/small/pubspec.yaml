name: fixture_small
description: Small integration fixture for dart_mutant — 6 construct types, ~13 mutants, 100% kill.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.24.0