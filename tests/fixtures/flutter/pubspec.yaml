name: fixture_flutter
description: Minimal Flutter fixture for dart_mutant — widget tests exercising the flutter test execution path (LCOV baseline, full-suite routing).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
